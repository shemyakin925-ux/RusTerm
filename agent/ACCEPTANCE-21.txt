Приёмка EquityLab — 2026-09-12 22:03
ветка: HEAD   HEAD: 6fe38f3

12. Скрипт приёмки не изменён
  OK    agent/acceptance.sh совпадает с origin/main

1. Каждый модуль rusterm/ импортируется
  OK    все модули импортируются (Python 3.14.6)

2. tests/test_invariants.py: пятнадцать инвариантов
  OK    все пятнадцать test_i01_…test_i15_ присутствуют

3. pytest целиком
          .....................................................................s.. [ 81%]
          ........................................................................ [ 95%]
          .........................                                                [100%]
  OK    pytest, код возврата 0

4. У каждого xfail непустая причина
  OK    все xfail с причиной (0 шт.)

5. Нет NotImplementedError / TODO / FIXME в rusterm/
  OK    заглушек нет

6. Ни строки Qt
  OK    Qt не импортируется нигде

7. SQL только в rusterm/store/
  OK    SQL не выходит за rusterm/store/

8. HTTP-библиотеки только в rusterm/providers/
  OK    HTTP не выходит за rusterm/providers/

9. Провайдеры не импортируют rusterm.store.db (I10)
  OK    провайдеры не знают о хранилище

10. docs/ не изменён, кроме новых ADR
  OK    docs/ нетронут, новых ADR: 1

11. Тесты проходят без zstandard (обязателен gzip-фолбэк)
          .....................................................................s.. [ 81%]
          ........................................................................ [ 95%]
          .........................                                                [100%]
  OK    без zstandard тесты зелёные

13. Ничего не осталось вне git
  OK    нет неотслеживаемых файлов и следов правки

Итог: пройдено 13, провалено 0
Принято.

─────────────────────────────────────────────────────────────────────
Прогон координатора (не отчёт исполнителя), 12.09.2026
ветка agent/night-3, HEAD 6fe38f3, Python 3.14.6, чистое дерево

Приёмка: пройдено 13, провалено 0, код возврата 0.

Полный набор тестов (junit-xml, не на глаз):
  tests=529  failures=0  errors=0  skipped=3
Три пропуска названы поимённо, все — окружение координатора:
  tests/test_llm_real.py::test_m5_real_model_skips_without_key
  tests/test_market_kr.py::test_live_company_json_answers   (нет ключа DART)
  tests/test_raw_store.py::test_put_object_labels_zstd_when_available
Исполнитель отчитался «526 passed, 3 skipped» — 526+3=529, сходится
ровно.

Схема: _SCHEMA_VERSION = 40, не менялась.
acceptance.sh: побайтово совпадает с origin/main (проверка 12 зелёная,
и отдельно git diff origin/main -- agent/acceptance.sh пуст).
docs/: нетронут, ровно один новый ADR (0013).
Золотые файлы: golden_m2.json и golden_m6_ca.json не тронуты
(git diff --stat origin/main..HEAD по ним пуст).

Снятые assert'ы проверены построчно — ни одного ослабления:
  tests/test_budget.py — пин эпохи пустых мест
  ('provider_not_implemented:dart') заменён на lifecycle-форму: для
  каждого из четырёх имён место ведёт себя честно в обеих фазах,
  и в обеих RequestGate не трогается. Опровергнут фактом L1, а не
  удобством.
  tests/test_add_refusal.py — 'assert code == 1 (format_unsupported)'
  заменён на 'assert code == 0': L5/L6 научили ступень ① читать txt.
  Существенная часть проверки (sha базы до/после dry-run) осталась.
  tests/test_currency_firewall.py — не переписан, а создан: на
  origin/main файла нет.

─────────────────────────────────────────────────────────────────────
Мерцание приёмки 12.09.2026: причина найдена, это не дефект ночи

Первый прогон координатора дал 13/0. Второй — 12/1, провалилась
проверка 13 («ничего не осталось вне git»): в корне репозитория
появился неотслеживаемый rusterm.db.

Виноват координатор, не исполнитель. Файл создал мой собственный
вызов `python3 -m rusterm.cli markets`: у `--root` дефолт «.», и любая
команда открывает базу в текущем каталоге. Файл удалён, третий прогон
снова 13/0.

Но находка настоящая и остаётся: команда `markets`, которая только
печатает реестр, создаёт rusterm.db (и каталоги raw/, logs/, exports/)
в рабочем каталоге. Из корня репозитория это ломает проверку 13 у
любого, кто запустил программу «посмотреть». Заведено в BACKLOG как
B35 — не блокирует приёмку.
