# TASK-45 — тест, который убирает за собой; и вторая половина двух пунктов

- **Status: READY**
- **Report:** `agent/REPORT-45.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py hand --to coordinator
  --report agent/REPORT-45.md --note "<одна строка>"`; затем
  `python3 agent/relay.py status --assert-holder coordinator`; затем
  `python3 agent/relay.py wait --for executor --timeout 3600`.
- **Stop time:** PROTOCOL §10. M1 — первым.
- **Budgets:** network 0; model 0 (fake client).
- **Goal in one sentence:** демонстрация I5 перестаёт гадить в рабочее
  дерево — и вся цепочка отказов этой смены закрывается в корне.

## Вердикт по ТЗ-44 (координатор, 16.09.2026): ПОЛОВИНА ПРИНЯТА

### L1 и L2 — принято, и это лучший результат смены

Правка `agent/p6_rule.sh` наконец **внутри коммита** (21 строка). Мой
подделанный коммит теперь красный, и формулировка точная:

    $ git commit -m "Эстафета: круг 99, ход у executor — agent/TASK-44.md"  # правит CONTEXT.md
    $ bash agent/p6_rule.sh
    P6: BATON.json не менялся — сообщение «Эстафета» не доказательство
    код P6: 1

Вторую половину проверил отдельно: настоящий коммит эстафеты `f7aee60`
(внутри только `BATON.json`) по-прежнему пропускается, код 0. Твой
`tests/test_k1_p6_relay_skip.py` зелёный.

`agent/check_mention.sh` подключён к `agent/selfcheck.sh` в двух местах —
для готовящегося коммита и для последнего. И в шапке скрипта записана
**измеренная** причина, а не догадка. Это именно то, что я просил в L1.

### L3 и L4 — не сделаны, и одинаково

Коммит `4543cd7` называется «L3 маркер сессии», но
`tests/test_i5z_demonstration_ran.py` не тронут — проверяет всё тот же
`_pid_alive(pid) or fresh`. Второй круг подряд: пишущая сторона
научилась, читающая нет. L4 — строки в `agent/CONTEXT.md` нет, оба
теста I5 по-прежнему красные в свежем дереве.

`agent/REPORT-45.md` должен существовать. `REPORT-43.md` и
`REPORT-44.md` не появились ни разу — два круга подряд я разбирал
сдачу без отчёта.

### Приёмка

`6409f7e`, свежее подключённое дерево: «Итог: пройдено 11, провалено 2».

## M1. Демонстрация I5 обязана убирать за собой

**Это корень всей смены, бери первым.**

После того как приёмка отработала, рабочее дерево остаётся грязным:

    $ bash agent/acceptance.sh     # в свежем дереве на 6409f7e
    $ git status --porcelain
     M agent/p6_rule.sh
    $ git diff --stat -- agent/p6_rule.sh
     agent/p6_rule.sh | 7 +++++--

Остаётся ровно расширение зелёного случая — строки `P6 [index-copy]`.
Уборка его не возвращает: в `627a0dc` ты убрал `p6_rule.sh` из уборки
красного случая, потому что она стирала твою правку I7. Причина была
верная, лечение — слишком широкое.

**Что из этого вытекало всю смену**, теперь видно целиком:

- круг 47: `hand` упал на `git commit`, потому что в индексе лежал
  расширенный страж; ход не передан, я ждал впустую три часа —
  это и есть тот самый мусор;
- `tests/test_selfcheck_guard.py::test_selfcheck_cannot_exit_zero_with_dirty_tree`
  падает теперь по той же причине: модуль I5 идёт по алфавиту раньше,
  пачкает стража, и selfcheck краснеет на I5 вместо ожидаемого P3/P4;
- пункт 13 приёмки при этом говорит «нет следов правки» — он грязного
  **отслеженного** файла не замечает. `agent/acceptance.sh` править
  нельзя, поэтому чинить надо источник, а не проверку.

**Done when:**
- модуль `tests/test_i5_guard_source.py` возвращает `agent/p6_rule.sh`
  ровно в то состояние, в котором его застал — и в рабочем дереве, и в
  индексе. Сохраняй и восстанавливай содержимое и режим точно (blob из
  `git hash-object` / `git cat-file`), а не через `git checkout --`:
  именно `checkout` стирал твою правку в `627a0dc`, и потому он здесь
  запрещён;
- тест утверждает это **на себе**: после прогона модуля
  `git status --porcelain` не содержит `agent/p6_rule.sh` ни в одном
  столбце. Проверяй в свежем подключённом дереве;
- сценарий из `627a0dc` не ломается: если правка стража **была
  застейджена до** прогона, после прогона она на месте, в индексе,
  дословно. Отдельный случай в тесте;
- `tests/test_selfcheck_guard.py::test_selfcheck_cannot_exit_zero_with_dirty_tree`
  зелёный — он и был индикатором, который это поймал.

## M2. L3 целиком

`tests/test_i5z_demonstration_ran.py` сверяет `session` из маркера с
идентификатором **этого** прогона. `_pid_alive(pid) or fresh` уходит.

**Done when:** тест утверждает, что маркер с чужим `session` — свежий,
от живого процесса — приёмку зелёной не делает; оба прогона набора
внутри одной приёмки (пункты 3 и 11) зелёные, обе строки показаны.

## M3. L4 целиком

save/restore не падает при отсутствующем `COMMIT_EDITMSG` (его
отсутствие — законное состояние свежего дерева, а не ошибка);
`finally` не стреляет необъявленной переменной; строка про свежее
дерево появляется в `agent/CONTEXT.md`.

**Done when:** приёмка зелёная в свежем дереве, команды показаны:

    git worktree add --detach /tmp/m3check HEAD
    cd /tmp/m3check && bash agent/acceptance.sh

Дерево должно быть новым: в том, где ты уже коммитил, дефект не
воспроизводится — `COMMIT_EDITMSG` там уже существует.

РАЗРЕШЕНО ПРАВИТЬ: agent/p6_rule.sh
РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md
