# TASK-70 — восемь оставшихся пустых; и проверка, которую мы чуть не пропустили

- **Status: READY**
- **Report:** `agent/REPORT-70.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py --branch agent/night-11
  hand --to coordinator --report agent/REPORT-70.md --note "<строка>"`;
  затем `python3 agent/relay.py --branch agent/night-11 wait
  --for executor --timeout 3600`.
- **Stop:** дневной круг — по списку, не по часам.
- **Budgets:** Q3 — до 20 живых запросов; остальное network 0.

## Q0. Вердикт по ТЗ-69: принято целиком

Прогон 13/0. Завышение ушло: `pe` 166.4 → **43.84**, `ps` 44.9 →
**11.80**. Годовое окно видно в lineage, страж сверяет входы со
словарём и показан красным на старом `pe`, согласованность двух
путей с допуском 5% обоснована сдвигом TTM-окон.

Урок круга записан в `CONTEXT.md` как правило: **зелёная приёмка не
значит верные числа.** Ловится это сверкой меры с определением и
двумя путями расчёта, а не границами «от и до».

## Q1. Распространить проверку двух путей

`pe` теперь сверяется двумя путями. Остальные меры — нет.

**Done when:**
- пройдено по словарю: для каждой меры сказано, есть ли второй путь
  расчёта из тех же данных (например `ev` через капитализацию плюс
  долг против `ev` через компоненты; `fcf` против `ocf - capex`);
- где второй путь есть — добавлена сверка с названным допуском,
  красная при расхождении, с обоими числами в сообщении;
- где второго пути нет — строка в отчёт, без выдумывания;
- **ни одного нового «разумного диапазона»**: сравниваем расчёт с
  расчётом, а не с ожиданием.

## Q2. Восемь оставшихся пустых мер

После ТЗ-69 из 28 мер со значением 20. Остальные восемь — назови их
поимённо и разложи как в N1: чинится картой, чинится расчётом,
честно невозможно.

**Done when:**
- по каждой из восьми названа причина и корзина;
- корзина «чинится картой или расчётом» закрыта в этом круге, если
  это не требует нового источника данных; закрытое — золотым тестом;
- `invested_capital` и `roic` разбери отдельной строкой: в ТЗ-68 ты
  сам предложил сумму трёх тегов — реши, это определение словаря или
  подстановка, и действуй по правилу ТЗ-56 Z1;
- итог числом: было 20 из 28, стало N.

## Q3. Второй эмитент США

Всё измерено на AAPL. Одна бумага — не проверка.

**Done when:**
- тот же путь пройден на **другом** эмитенте США с иной структурой
  отчётности (выбор твой, довод в отчёт: другой сектор, наличие
  привилегированных акций или неконтролирующих долей);
- таблица «мера → значение либо причина» по обоим эмитентам рядом;
- различия объяснены: где меньше мер — почему именно, тегами;
- если на втором эмитенте вылезает дефект, невидимый на AAPL, — это
  главная находка круга, первой строкой отчёта.

## Q4. Слияние в main

Ветка ушла от `main` на два десятка кругов.

**Done when:** в отчёте одной строкой: что должно попасть в `main`
из этой ветки и есть ли что-то, чего пускать туда **не** следует
(временные стабы, отладочные ветки кода). Слияние делаю я.

## Если всё закрыто

Бери из BACKLOG пункт S или M, названный в отчёте.

РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md, GUIDE.md
