# TASK-96 — сверка: что из ТЗ-74 и ТЗ-77 уже сделано, что устарело, что делать

- **Status: ACCEPTED** (round 122, 25.09)
- **Report:** `agent/REPORT-96.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py --branch agent/night-11
  hand --to coordinator --report agent/REPORT-96.md --note "<строка>"`;
  **затем сразу** `python3 agent/relay.py --branch agent/night-11 wait
  --for executor --timeout 3600` — и не завершай сессию.
- **Budgets:** до **60** живых запросов на всё ТЗ (R3); остальное network 0.
- **Как работать:** вопросов не задавать, спорное — в Disputed и
  дальше; один пункт — один коммит с именем пункта в заголовке.
- **Заменяет:** `agent/TASK-74.md` и `agent/TASK-77.md` целиком. Сами
  файлы не правятся (старые ТЗ не переписываются, указание пользователя
  23.09) — **их отдельно не бери**, всё нужное из них собрано здесь.

РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md
РАЗРЕШЕНО ПРАВИТЬ: GUIDE.md

## R0. Зачем сверка

ТЗ-73, 74 и 77 написаны до кругов 109–113 на ложной посылке «базы из
пяти бумаг больше нет». С тех пор часть их закрыта другими заданиями,
часть устарела. Без сверки ты потратил бы круг на сделанное.

**Решение пользователя от 24.09: ТЗ-73 делает координатор сам.** Здесь
его пункты не трогаются; там, где ТЗ-74 зависит от ТЗ-73 (вкладки
«Отрасль» и «Качество»), — см. R4.

**Факты, измеренные координатором на `a258537`:**

| Что | Состояние |
|---|---|
| База пользователя | `/Users/anton/equitylab`, схема 45, шесть бумаг AAPL ADBE KSPI MSFT VALE VZ, все на рынке `US` (ТЗ-81 B0) |
| Один каталог у CLI, окна, `.app` | **сделано** — ТЗ-90 A5 |
| Честные колонки лет | **сделано** — ТЗ-81 B2, ТЗ-95 F3 |
| Окно на старой схеме | **сделано** — ТЗ-95 F1 |
| Одна команда «полного пути» (`follow` или аналог) | **нет**; есть `census`, но ТЗ-94 пишет, что `census --rebuild` портит данные — на нём не строить |
| Тест первого часа | **частично**: `tests/test_task65_k4_firsthour.py` |
| Раздел «первые пятнадцать минут» в `GUIDE.md` | **нет** |

## R1. Таблица сверки — первым коммитом

**Done when:** в отчёте таблица: каждый пункт ТЗ-74 (U1–U4) и ТЗ-77
(X0–X4) → **сделан** (каким коммитом, какой командой это видно) /
**устарел** (почему) / **открыт** (каким пунктом этого ТЗ закрывается).
Моё прочтение ниже — проверь его, а не перепиши: где я не прав, это
Disputed с доказательством.

| Пункт | Прочтение координатора |
|---|---|
| 77 X0 (посылка «базы нет») | устарел — ТЗ-81 B0 |
| 77 X1 (одна команда пересобирает базу) + 74 U1 (одна команда полного пути) | **одно и то же** — делается один раз, R2 |
| 77 X2 (живой прогон ≤ 60) + 74 U2 (прогон на бумагах пользователя ≤ 40) | **один** живой прогон, ≤ 60 на всё, R3 |
| 77 X3 (эталон офлайн из фикстур) | открыт — R3 |
| 77 X4 (какие критерии 73/74 прогоняемы) | заменён этой таблицей |
| 74 U3 (первый час по всем вкладкам) | частично — R4 |
| 74 U4 («первые пятнадцать минут» в GUIDE) | открыт — R5 |

## R2. Одна команда: пустой каталог → полный экран

**Done when:**
- один вызов (имя твоё) проводит бумагу весь путь: поиск в SEC →
  отчётность → цены → снапшот; печатает стадии с числами, на каждом
  отказе — исполнимую строку-совет (разбирается парсером CLI);
- **идемпотентен**: второй прогон не портит данные и стоит заметно
  меньше запросов — оба числа в отчёт;
- **не построен на `census --rebuild`**; если переиспользуешь его части —
  докажи, что порча данных из ТЗ-94 тебя не касается;
- отрасль и governance в этот путь **не входят** — это ТЗ-73; команда
  лишь говорит, что эти стадии ещё не собираются, словами.

## R3. Живой прогон и эталон офлайн

**Правило P7 здесь главное.** Прогон идёт в каталог под `/tmp` с
`--root`, **никогда** не в `~/equitylab` и не в `~/.rusterm`. Базу
пользователя можно только **читать** (`sqlite3 … mode=ro`) — для
сравнения чисел.

**Done when:**
- команда R2 прогнана живьём по шести бумагам пользователя (AAPL ADBE
  KSPI MSFT VALE VZ) в `/tmp`-каталог, не больше 60 запросов на всё;
- таблица в отчёте: бумага → мер со значением → лет истории →
  запросов; рядом — те же числа из базы пользователя (только чтение),
  и расхождения объяснены источником;
- ответы записаны фикстурами (≤ 256 КБ на файл, `trim_companyfacts`),
  офлайн-прогон из них пересобирает ту же базу, тест утверждает числа,
  **ноль запросов** доказан счётчиком `RequestGate`.

## R4. Первый час по вкладкам — то, что уже можно проверить

**Done when:** маркерный тест первого часа проходит путь R2 и
утверждает **содержимое** вкладок «Компания» и «Настройки». Для
«Отрасли» и «Качества» — `xfail(strict=True, reason="ТЗ-73 …")`: тест
написан сейчас и сам покраснеет, когда ТЗ-73 их наполнит, а не будет
дописан задним числом.

## R5. «Первые пятнадцать минут» в GUIDE

**Done when:** в `GUIDE.md` раздел: команда R2, что видно после неё,
строка `RUSTERM_DATA=…` в `~/.rusterm.env` (ТЗ-90), двойной щелчок по
`.app` и что делать, если окно говорит про старую схему (ТЗ-95). Все
вставки — из настоящего прогона R3 (страж `test_guide_truth`).

## R6. Кто что делает: координатор работает параллельно

С 24.09 координатор по решению пользователя сам делает трудные
задания — в своих ветках `coord/…`, вливая их в рабочую ветку в свой
ход. Чтобы не сделать одно дважды и не столкнуться в одних файлах:

| Пункт | Кто |
|---|---|
| ТЗ-73 целиком | координатор |
| ТЗ-87 `G2` (SIC → сектор) и `G3` (почему у KSPI ноль) | **координатор** — часть ТЗ-73 и починки бедных мер; **не бери** |
| ТЗ-87 `G1` (перепись синонимов) | ты, в своей очереди |
| ТЗ-91, ТЗ-94 `E1`/`E2` | координатор |
| ТЗ-94 `E3`–`E8` | ты, в своей очереди |
| ТЗ-92 | ты — но см. ниже про `C1` |

**Уже сделано координатором и влито в рабочую ветку** (`coord/thin-
snapshots`, коммит `fd40f7a`): регрессия ТЗ-78 `Y2` — дата обложки
`dei` сдвигала конец периода подачи, и почти каждый финансовый факт
записывался `restated`; снапшот брал только `as_reported`, у половины
аналогов пользователя было 4 меры из 28. Починен разборщик (проход 1
не учитывает `dei`) и данные (новая команда `rusterm reparse`). На базе
пользователя мер со значением стало 592 вместо 334.

Вторая влитая ветка — `coord/dividends` (`835425b`): дивидендная
доходность из отчётности SEC — сумма четырёх подряд идущих кварталов
или свежий годовой dps (бесплатный Twelve Data на дивиденды отвечает
403). div_yield у 24 бумаг из 44 вместо одной; мер со значением 627.

**Для ТЗ-92 `C1`** (дедупликация сохраняет исходную подачу): это тот же
разборщик и тот же файл `rusterm/parsers/__init__.py`. Строй поверх
`fd40f7a`, а не рядом; прежде чем начать — перечитай проход 1 и
убедись, что `C1` ещё воспроизводится после починки `dei`. Если нет —
это результат: назови его в отчёте, не чини несуществующее.

## R7. Что не делать

- Не трогать `acceptance.sh`, `selfcheck.sh`, `p1_rule.sh`,
  `p6_rule.sh`, `githooks/`.
- Не править `PROTOCOL.md`, `BACKLOG.md`, `LAUNCH.md`, `TASK-*.md`.
- **Не писать** в `~/equitylab` и `~/.rusterm` (P7).
- Не брать пункты ТЗ-73 — их делает координатор.

## Если всё закрыто — твоя очередь (решение пользователя, 24.09)

Координатор параллельно делает сам: **ТЗ-73** (вместе с ТЗ-87 `G2`/`G3`),
**ТЗ-91**, **ТЗ-94 `E1`/`E2`**. Эти пункты не бери. Твоя очередь по порядку:

| # | Задание | Что именно |
|---|---|---|
| 1 | `agent/TASK-88.md` | целиком |
| 2 | `agent/TASK-89.md` | целиком |
| 3 | `agent/TASK-92.md` | целиком; `C1` — поверх `fd40f7a` (см. R6) |
| 4 | `agent/TASK-93.md` | целиком |
| 5 | `agent/TASK-87.md` | **только `G1`** (синонимы) |
| 6 | `agent/TASK-94.md` | **только `E3`–`E8`** |
| 7 | `agent/TASK-85.md`, `agent/TASK-86.md` | целиком |
| 8 | `agent/BACKLOG.md` | сверху вниз пункты размера S |

ТЗ-88 и ТЗ-89 подняты вперёд намеренно: это дефекты самой эстафеты,
которые уже трижды её останавливали.

Каждое ТЗ очереди — отдельный круг: сдал (`hand`), встал в `wait`,
координатор принимает и передаёт следующее. Номер в `BATON.json` —
главнее этой таблицы.
