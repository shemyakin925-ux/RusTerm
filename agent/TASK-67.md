# TASK-67 — проверить, что щель закрыта; и вернуться к продукту

- **Status: READY**
- **Report:** `agent/REPORT-67.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py --branch agent/night-11
  hand --to coordinator --report agent/REPORT-67.md --note "<строка>"`;
  затем `python3 agent/relay.py --branch agent/night-11 wait
  --for executor --timeout 3600`.
- **Stop:** дневной круг — по списку, не по часам.
- **Budgets:** M2 — до 20 живых запросов; остальное network 0.

## M0. Вердикт по ТЗ-66: принято целиком

Прогон в чистом клоне: **13/0, exit 0**. Проверено отдельно то, что
возвращалось: K3 и K5 зелёные **в одиночку**, а не только в наборе.
Страж поимённо осуждает коммиты-нарушители (`8ea2e0d`, `b3b55ef`).
Работа приехала своими коммитами с именами пунктов — как и просили.

Три круга подряд машинерия отъедала время. Она закрыта: красное
больше не уезжает на ветку, потому что `hand` его не пускает.
**Возвращаемся к продукту.**

## M1. Щель закрыта — докажи это, а не объяви

Прежде чем строить дальше, покажи, что новый заслон работает на
настоящей попытке, а не в теории.

**Done when:**
- поставлен эксперимент: нарочно красное дерево (сломай один тест
  временно) → `relay.py hand` **отказывается** передать ход, коммит
  не создан, ветка не тронута; вывод приложен дословно;
- второй эксперимент: коммит с заголовком «Эстафета:» рукой, с
  работой в индексе → красный страж, с именами файлов;
- дерево после экспериментов возвращено байт в байт (покажи чем);
- одна строка в отчёте: что теперь **физически** невозможно, а что
  по-прежнему держится на дисциплине.

## M2. Цены: 20 мер из 28 всё ещё пусты

ТЗ-63 намерил 8 значений из 28, ТЗ-64 довёл до 10. Главная дыра та
же: ценовые меры пусты, потому что цены — отдельный канал, и
пользователь о нём узнаёт только из совета в сноске.

**Done when:**
- сценарий первого часа расширен ценами: после `ingest --source
  edgar` идёт `ingest --source twelvedata` на том же эмитенте;
  измерено числами, сколько мер стало со значением;
- если ценовой канал упирается в лимит бесплатного тарифа (8/мин,
  800/сут) — это названо числом и обойдено ожиданием, а не
  выдумкой;
- тест `firsthour` (маркер) расширен тем же шагом на записанных
  ответах; числа в отчёте обновлены: было 10 из 28, стало N;
- если какие-то меры остаются пустыми **после** цен — названы
  поимённо с причиной; это материал следующего ТЗ.

## M3. Что видит человек, у которого ничего нет

Первый час меряли с готовым `~/.rusterm.env`. У нового пользователя
его нет.

**Done when:**
- пройден сценарий с **пустым** окружением: ни одного ключа;
  записано дословно, что программа говорит на каждом шаге;
- каждый отказ из-за отсутствующего ключа несёт: что именно нужно,
  где взять, в какую переменную положить — теми же словами, что
  KR-дверь из ТЗ-61 F4;
- SEC-контакт (`RUSTERM_SEC_UA`) — отдельный случай: это не ключ, а
  требование вежливости SEC. Проверь, что об этом сказано понятно и
  что без него не происходит молчаливого падения;
- места, где человек упрётся и не поймёт, — списком, не чинить.

## M4. GUIDE догоняет продукт

За последние круги появились: окно, `rusterm desktop`, степени
каналов, советы в отказах, подписи бюджета, стадии сбора.

**Done when:** `GUIDE.md` описывает их так, как они работают сейчас;
каждая вставка — из настоящего прогона (страж `test_guide_truth`
это проверяет); ни одной строки про то, чего нет.

## Если всё закрыто

Бери из BACKLOG пункт S или M, названный в отчёте.

РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md, GUIDE.md
