# TASK-59 — мера без хозяина; и рынок, который умеет только сырьё

- **Status: READY**
- **Report:** `agent/REPORT-59.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py hand --to coordinator
  --report agent/REPORT-59.md --note "<одна строка>"`; затем
  `python3 agent/relay.py status --assert-holder coordinator`; затем
  `python3 agent/relay.py wait --for executor --timeout 3600`.
- **Stop:** если круг дневной — по списку, а не по часам (вердикт Q1
  в ТЗ-58 C0). Если ночной — PROTOCOL §10, число командой ТЗ-55 Y0.
- **Budgets:** D1, D3, D4 — network 0; D2 — до 20 запросов.
- **Goal in one sentence:** ни одна мера не зеленеет молча, и рынок,
  который умеет только сырьё, говорит это сам.

Берётся, когда `agent/TASK-58.md` закрыт целиком.

## D1. Перепись «зелёных, которые не заслужили»

C4 снял `clip()` у `effective_tax`. Вопрос шире: **где ещё** мера
подгоняет значение вместо отказа.

**Done when:**
- пройдено по всем формулам `rusterm/formulas.py`: для каждой меры
  названо, что она делает со входом вне ожидаемой области — отказ,
  зажим, деление на ноль, молчаливый ноль, `None`;
- таблица «мера → поведение вне области → это честно/нечестно» в
  отчёте;
- каждый **нечестный** случай закрыт отказом с причиной из словаря и
  тестом на граничный вход. Если случаев больше пяти — закрой пять
  самых дешёвых и перечисли остальные строками;
- ни одно существующее золото не переписано без строки в сообщении
  коммита, объясняющей, какое значение и почему изменилось.

## D2. Рынок, который умеет только сырьё, говорит это сам

После ТЗ-57 A4 у AU канал есть, но фактов он не даёт: каждая подача —
`manual_import_required`. Это честно внутри, но снаружи `rusterm
markets` показывает канал наравне с теми, что дают меры.

**Done when:**
- в реестре рынков у канала есть различимая **степень**: сырьё /
  факты / меры, — и `rusterm markets` её показывает;
- степень вычисляется из того, что канал реально произвёл, а не
  записана рукой. Тест на это: рынок, у которого не появилось ни
  одного факта, не может показать степень «факты»;
- `rusterm markets --json` несёт то же поле; схема ключей закреплена;
- строка M8 в `agent/CONTEXT.md` приведена к новому виду.

## D3. Причины, которых нет в словаре

`rusterm/reasons.py` — закрытый словарь. За смены ТЗ-56-58 в код
приехали новые каналы (cvm, asx, dart) и новые отказы.

**Done when:**
- прогоном собраны **все** строки причин, которые код способен
  вернуть (обход по источникам, не по памяти), и сверены со словарём;
- каждая причина вне словаря либо заведена в него, либо заменена
  существующей;
- есть тест, который краснеет при появлении причины вне словаря, и
  он покрывает новые каналы, а не только старые.

## D4. Хвост таблицы C3

Строки таблицы «команда → как открывает», которые ТЗ-58 C3 не успел
закрыть. Та же механика, assert на команду.

## Если всё закрыто

`agent/BACKLOG.md`, пункт S или M. Правку BACKLOG не вноси — назови
строкой в отчёте (вердикт Q4).

РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md
