# TASK-61 — три лица одной правды; и десктоп, который не срезает углы

- **Status: READY**
- **Report:** `agent/REPORT-61.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py --branch agent/night-11
  hand --to coordinator --report agent/REPORT-61.md --note "<строка>"`;
  затем `python3 agent/relay.py --branch agent/night-11 wait
  --for executor --timeout 3600`.
- **Stop:** дневной круг — по списку, не по часам.
- **Budgets:** network 0 везде, кроме F4 (до 10 запросов).

## F0. Вердикт по ТЗ-60

**Принято.** E1-E7 закрыты, прогон в чистом клоне подтверждает.
Отдельно отмечаю три вещи, потому что они редкие:

- круг 73 ты вернул пустым, но **правильно**: отказался от
  `--no-verify` и от трюков с деревом, назвал причину и вернул
  эстафету. Простой из-за моей ошибки дешевле обойдённого стража;
- E1 разобран до причин, а не до симптомов: кэш `origins` в
  `load_env` — процесса, а не теста; страж координаторских файлов
  выходит раньше стража чистоты на merge-HEAD; каскад через
  вложенную приёмку. Это образец того, как закрывать мерцание;
- **находка внутри E1 важнее самого E1**: полоса C ввела пять причин
  (`no_data_dir`, `synthetic_demo_only`, `index_unavailable`,
  `fetch_failed`, `unexpected_error`) **мимо закрытого словаря**.
  Словарь — не формальность: он и есть обещание, что отказ честен.
  Отсюда первый пункт.

BACKLOG правлю я: B34, B35, B36, B37, B39, B40 закрыты твоими
строками, B38 помечена координаторской. Больше в очереди ничего нет.

## F1. Десктоп под теми же правилами, что и ядро

Пять причин мимо словаря — симптом. Проверь, где ещё окно живёт по
своим правилам, а не по общим.

**Done when:**
- пройдено по `rusterm/desktop/` целиком и в отчёт выложена таблица:
  где окно **считает** что-либо само (формула, агрегат, округление,
  проценты), где **формулирует** текст отказа само, где обращается к
  данным мимо `rusterm/tui/model.py` и репозиториев;
- каждый случай счёта в окне либо перенесён в ядро, либо назван в
  Disputed с доводом, почему это отображение, а не расчёт;
- **ни одной новой причины мимо словаря**: тест, который краснеет,
  если `rusterm/desktop/` возвращает строку отказа, которой нет в
  `rusterm/reasons.py` — не список из пяти, а обход по коду;
- округление и форматирование чисел в окне не меняют значения:
  тест на меру, где третий знак важен.

## F2. Три лица одной правды

E5 сверил окно с CLI. Третье лицо — TUI — осталось в стороне, и
именно там прошлой осенью нашлись расхождения.

**Done when:**
- тест сверяет **три** источника на одном эмитенте и одной мере:
  `rusterm export`, `rusterm/tui/model.py`, `rusterm/desktop/`;
  значения равны байт в байт, отказы — одним токеном;
- расхождение — красный тест с тремя значениями в сообщении;
- прогон по трём рынкам: US, CA, BR;
- если какое-то лицо физически не показывает меру (нет экрана,
  нет колонки) — это записано строкой, а не обойдено молча.

## F3. Окно на настоящем объёме

ADR-0004 закладывал таблицы до 500 строк и ряды до 2500 точек.
Проверено это не было ни разу.

**Done when:**
- собран список наблюдения на 500 бумаг (синтетика допустима, но
  данные — из настоящего пути, а не из воздуха);
- измерено и названо числами: время открытия окна, время переключения
  компании, время построения графика на 2500 точках;
- если что-то из этого делает окно неотзывчивым — названо, что
  именно, и вынесено пунктом; чинить в этом ТЗ не обязательно;
- тест по маркеру, который не гоняется в обычном наборе.

## F4. KR: дверь без ключа

`RUSTERM_DART_KEY` на машине нет — это измерено тобой и принято.
Вопрос не в ключе, а в том, что видит пользователь.

**Done when:**
- `rusterm markets` и окно показывают KR со **степенью «нет ключа»**,
  а не с обещанием мер (механика E4);
- попытка сбора по KR даёт отказ с причиной из словаря и строкой,
  **что именно сделать** — где взять ключ, в какую переменную
  положить; строка проверена тестом на подстановку, не на глаз;
- ни одного живого запроса к DART без ключа: тест на то, что до сети
  дело не доходит;
- `agent/CONTEXT.md` §5 приведена к факту: какие ключи есть на самом
  деле, какие нет.

## Если всё закрыто

Очередь BACKLOG пуста: единственный открытый пункт — координаторский.
Закончив, напиши строкой, что взял бы следующим, и сдавай эстафету.

РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md, GUIDE.md
