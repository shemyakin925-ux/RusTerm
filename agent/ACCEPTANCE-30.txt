ПРИЁМКА ТЗ-29, ТЗ-28, ТЗ-30 — 14.09.2026, координатор
======================================================

ЧТО ПРОГНАНО, А НЕ ПРОЧИТАНО
-----------------------------
Ветка agent/night-10, HEAD 8a17b8a, ключи пользователя в окружении:
  bash agent/acceptance.sh   -> Итог: пройдено 13, провалено 0; EXIT=0
  проверка 12 (acceptance.sh совпадает с origin/main) -> OK
  проверка 10 (docs/ нетронут) -> OK, новых файлов-добавлений 0
  pytest --collect-only      -> 639 (пять live-тестов по умолчанию
                                не собираются — маркер введён ТЗ-29 A2)
  git diff main..HEAD -- tests/ | grep '^-.*assert' -> одна строка:
    assert len(names) == 8 внутри удалённого test_available_lists_
    all_eight_names; замена строго сильнее (см. вердикт 1).

СОСТОЯНИЕ ПО ОТЧЁТАМ (сверено с кодом точечно)
-----------------------------------------------
ТЗ-29 DONE  A1..A5: conftest изолирует окружение, маркер live в
            addopts (-m "not live"), пять живых тестов помечены,
            repr клиентов маскирует ключ, приёмка с ключами 13/13.
ТЗ-28 DONE  R1..R6: тариф — поле реестра (_TIERS/_CHANNEL_TIERS),
            paid_channel_refused значением, doctor печатает раздел
            бесплатности, страж хостов мимо реестра (ast, доксстринги
            исключены), GUIDE называет цену каждого ключа.
ТЗ-30 DONE  B1..B4 + ремонт: живой сбор 5000 строк AAPL одним
            запросом (2006-10-25..2026-09-11), повтор — 0 запросов и
            0 строк, три именованных отказа вендора, кадентность на
            живых строках. Ремонт: SQL кеш-lookup уехал из cli в
            RawRepo.find_by_provider_url — нарушение I9 поймала сама
            приёмка (проверка 7), исправлено в ту же смену.

ВЕРДИКТЫ (полностью — в шапке agent/BACKLOG.md)
------------------------------------------------
1. R3: Done-when моего же ТЗ противоречил его рабочей секции.
   ИСПОЛНИТЕЛЬ ПРАВ. Обе замены строго сильнее. Общее правило: пин
   над реестром выводится из реестра -> ТЗ-40 L7.
2. Пин места twelvedata заменён после появления модуля — принято.
3. B33 закрыт ранее; ТЗ-40 L4 теперь про B34 и B35.
4. selfcheck печатает только хвост красной приёмки -> ТЗ-40 L5.
5. Флак test_m4_scale под нагрузкой -> ТЗ-40 L6 (форма, не секунды).
6. У кадентности нет команды -> ТЗ-31 C5.
7. Бесплатный тариф не присылает adjusted -> ADR-0019; ТЗ-31 C1
   переписан на три внутренних доказательства; NULL закреплён
   голден-тестом.
8. Засветившийся ключ OpenRouter — действие пользователя, в
   репозитории его нет (перепроверено).

ЧТО СДЕЛАНО КООРДИНАТОРОМ
--------------------------
- agent/night-10 влита в main перемоткой, main запушен.
- ADR-0019 (вендорский adjusted недоступен на бесплатном тарифе),
  README §15 называет его.
- ТЗ-31: C1 переписан, добавлен C5. ТЗ-40: L4 уточнён, добавлены
  L5, L6, L7.
- agent/CONTEXT.md обновлён: M9 с живыми строками, два новых
  стоячих правила (изоляция тестов и маркер live; тариф каналов).

ИТОГ: ПРИНЯТО.
