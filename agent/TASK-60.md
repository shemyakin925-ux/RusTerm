# TASK-60 — одна полоса; десктоп получает дверь, рынок — честную степень

- **Status: READY.** Заменяет `agent/TASK-59.md` (`Status: SUPERSEDED`:
  его D1 и D3 закрыты полосой B, ТЗ-B1 B1.1 и B1.2).
- **Report:** `agent/REPORT-60.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py --branch agent/night-11
  hand --to coordinator --report agent/REPORT-60.md --note "<строка>"`;
  затем `python3 agent/relay.py --branch agent/night-11 wait
  --for executor --timeout 3600`.
- **Stop:** дневной круг — по списку, не по часам.
- **Budgets:** network 0 везде, кроме E3 (до 10 запросов, если
  понадобятся свежие данные).

## E0. Где мы и что изменилось

**Параллельных полос больше нет.** Три ветки смены слиты в одну
(PR #9): полоса A — рынки и меры, B — честность формул и словарь
причин, C — десктоп на PySide6 (`rusterm/desktop/`, ТЗ-C1…C10).
Конфликтов в коде не было. Работаешь один, на `agent/night-11`,
территория — весь репозиторий.

**Вердикты по ТЗ-58, спорить не нужно:**

- ТЗ-58 **принято**: прогон в чистом клоне дал 13/0, exit 0.
- **C7 принято как измерено.** Моя гипотеза про три названных теста
  снята: ты показал прогоном, что воспроизводится красный O0, а те
  три — нет. Больше к ним не возвращаемся.
- **C5 принято.** Тела документов ASX недостижимы с этой сети,
  `manual_import_required:asxdoc:<ключ>` — честный отказ.
- **C2 (B36) принято.** Гвард поймал оба серых числа.
- **C1 заблокировано не по твоей вине.** Ключа DART в окружении нет —
  ты это измерил, а посылка ТЗ была неверной. Пункт снят с тебя до
  тех пор, пока ключ не появится. **Сам его не ищи и обходов не
  придумывай.**

## E1. У десктопа появляется дверь из CLI

ТЗ-C1 запретило трогать `rusterm/cli/__init__.py`, потому что файл был
занят другой полосой. Полос больше нет — запрет снят.

**Done when:**
- `rusterm desktop` зарегистрирована как команда и открывает то же
  окно, что `python3 -m rusterm.desktop`; обе точки входа ведут в один
  код, копии нет;
- без установленного PySide6 команда **не падает трассировкой**:
  говорит словами, что нужно поставить и какой группой зависимостей;
- команда сама по себе каталог данных не создаёт (правило B35);
- `GUIDE.md` дополнен разделом про окно: как запустить, что видно;
  вставки — из настоящего прогона, а не придуманные;
- в `agent/CONTEXT.md` §2 появилась строка про `rusterm/desktop/`.

## E2. Рынок говорит, на что он способен

После ТЗ-57 A4 у AU есть канал, но фактов он не даёт: каждая подача —
`manual_import_required`. Снаружи это неотличимо от рынков, дающих
меры.

**Done when:**
- у канала в реестре есть различимая **степень**: сырьё / факты /
  меры; `rusterm markets` и `--json` её показывают, схема ключей
  закреплена;
- степень **вычисляется из того, что канал произвёл**, а не записана
  рукой: рынок без единого факта не может показать «факты». Тест;
- десктоп показывает ту же степень теми же словами;
- строка M8 в `agent/CONTEXT.md` приведена к новому виду.

## E3. Окно и CLI показывают одно и то же

Самая дешёвая проверка правдивости десктопа — сравнение с CLI.

**Done when:**
- тест берёт эмитента со снапшотом и сверяет: значения мер в окне
  байт в байт равны тому, что даёт `rusterm export` для той же
  меры и периода;
- то же для отказов: где CLI говорит `missing_data: total_equity`,
  окно говорит «нет данных», а панель источника называет тот же
  концепт;
- расхождение — красный тест с обоими значениями в сообщении, а не
  подгонка окна под CLI или наоборот;
- прогон по трём рынкам: US, CA, BR.

## E4. Следы параллельных полос

**Done when:**
- `agent/STATE.json` и `agent/BATON.json` содержат состояние одной
  полосы, без унаследованных от соседних веток значений;
- в `agent/` не осталось файлов, относящихся к отменённым полосам,
  кроме отчётов (они история и остаются);
- в отчёте строкой: что нашёл и что удалил.

## E5. Ревизия BACKLOG

**Done when:** прошёл по `agent/BACKLOG.md` и назвал строками, какие
пункты закрыты работой трёх полос и какими коммитами. **Сам файл не
правь** — правку внесу я (вердикт Q4).

## Если всё закрыто

Бери из `agent/BACKLOG.md` пункт S или M, названный в отчёте.

РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md, GUIDE.md
