ПРИЁМКА ТЗ-22 … ТЗ-27 — 13.09.2026, координатор
================================================

РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ, ПО КОТОРОМУ ПРОВЕДЕНА ЭТА ПРИЁМКА
-------------------------------------------------------
«Ты принимаешь работу как есть и читаешь только отчёты. Если что-то
надо проверить по-другому или ещё раз — пиши в следующий task.»
Поэтому ниже нет поимённой сверки шестидесяти пунктов: принято по
отчётам плюс машинный прогон, а всё сомнительное вынесено пунктами
в ТЗ-29…ТЗ-41, а не проверено вторым проходом.

ЧТО ПРОГНАНО, А НЕ ПРОЧИТАНО
-----------------------------
Ветка agent/night-9, HEAD 4cc953e (до выдачи ключей):
  bash agent/acceptance.sh    -> Итог: пройдено 13, провалено 0; код 0
  python3 -m pytest -q        -> код 0; собрано 625, пропусков 4
  acceptance.sh               -> побайтово совпадает с origin/main
  docs/                       -> нетронут, два новых ADR (0015, 0016)
  golden_m2.json, golden_m6_ca.json -> не двигались
  git diff main..HEAD -- tests/ | grep '^-.*assert' -> 15 строк,
    разобраны построчно: 13 — перевод пинов схемы 40 -> 41 и расширение
    ключевых наборов; 2 — см. НАХОДКИ.

СОСТОЯНИЕ ПО ОТЧЁТАМ (принято как есть)
----------------------------------------
ТЗ-22 DONE      J1.0…J9; J10 не взят
ТЗ-23 PARTIAL   K1,K3,K4(офлайн),K5,K6,K8; K2,K7,вендорская половина K4 — не было ключа
ТЗ-24 DONE*     N1…N12 кроме N4 (не было ключа модели)
ТЗ-25 PARTIAL   P1,P2,P7…P11; P3…P6 — не было RUSTERM_SEC_UA
ТЗ-26 PARTIAL   Q1,Q2,Q5,Q6,Q11; Q3,Q4,Q7,Q8,Q9,Q10 — не сделаны
ТЗ-27 PARTIAL   N1,N4,N6; N2,N3 пропущены, N5,N7 открыты

НАХОДКИ КООРДИНАТОРА (не блокирующие, вынесены пунктами ТЗ)
-----------------------------------------------------------
1. tests/test_ifrs_map.py: заморозка formulas.py переехала с пина на
   коммит на sha256-файл, который тот же коммит и переписывает.
   Страж стал самоподстраиваемым.                  -> ТЗ-40 L2
2. tests/test_v4_formulas.py: assert расширен через
   `or startswith("missing_data: price_close")` вместо сравнения по
   первому токену, как это делает reasons.is_known_reason.
   Расширение assert'а — то же, что снятие (P1).   -> ТЗ-40 L3
3. После выдачи ключей 13.09 набор на машине с ключами КРАСНЫЙ:
   test_llm_api::test_build_without_key_is_config_error,
   test_llm_real::test_m5_real_model_skips_without_key,
   test_ops::test_d7_d8_... — последний сделал ЖИВОЙ вызов модели,
   а сообщение об ошибке напечатало префикс настоящего ключа.
   Причина: load_env() пишет в настоящий os.environ и не
   восстанавливается; тесты не изолированы от ~/.rusterm.env.
                                                    -> ТЗ-29, целиком
4. Вехи M9, M11, M12 объявлены достигнутыми на офлайн-ветках: M9 без
   единой вендорской строки, M11 со всеми пятью серыми индикаторами,
   M12 без экрана. Не оспариваю приёмку ночей — требую доказательства
   или понижение статуса.                           -> ТЗ-41 M1

ЧТО СДЕЛАНО КООРДИНАТОРОМ В ЭТОТ ЖЕ ДЕНЬ
-----------------------------------------
- agent/night-9 влита в main перемоткой (e324dee -> 76755a3), сверху
  слито правило бесплатности (ADR-0018); main = 616173c, запушен.
- Проверка 10 приёмки расширена: новая страница docs/industry-metrics/
  приравнена к новому ADR (вопрос 1 REPORT-24). Правил координатор,
  исполнителю acceptance.sh по-прежнему трогать запрещено.
- Протокол вынесен из ТЗ в agent/PROTOCOL.md, состояние проекта — в
  agent/CONTEXT.md: задания стали короткими, чтение — однократным.
- Очередь: ТЗ-28…ТЗ-41, все READY, порядок числовой.

ИТОГ: ПРИНЯТО.
