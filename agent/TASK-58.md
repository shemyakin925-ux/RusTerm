# TASK-58 — мера, которая выдумала ноль; Корея с ключом; живая модель под гвардом

- **Status: READY**
- **Report:** `agent/REPORT-58.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py hand --to coordinator
  --report agent/REPORT-58.md --note "<одна строка>"`; затем
  `python3 agent/relay.py status --assert-holder coordinator`; затем
  `python3 agent/relay.py wait --for executor --timeout 3600`.
- **Stop time:** PROTOCOL §10, число — командой из ТЗ-55 Y0.
- **Budgets:** C1 — до 25 запросов к DART через `RequestGate`;
  C2 — до 40 вызовов бесплатной модели OpenRouter; C3 — network 0.
- **Goal in one sentence:** «нет ключа» больше не причина: KR собирается
  через DART, а живая модель отвечает под тем же гвардом, что и офлайн.

Берётся, когда `agent/TASK-57.md` закрыт целиком.


## C0. Вердикты по Disputed из REPORT-57

Восемь вопросов, восемь ответов. Спорить по ним снова не нужно.

- **Q1 (время остановки).** Правило 10:00 по Дангану — про
  **автономную ночную смену**. Эстафета, переданная днём, — это
  дневной круг: работай до исчерпания списка ТЗ, а не до часа. Строку
  Y0 в дневном круге не пиши: пиши `Stop: дневной круг, по списку`.
  Отрицательное число минут — признак того, что правило применено не
  к тому кругу.
- **Q2 (знак 3.08).** Чинить **на слое карты** (`cvm-dfp.v2`) —
  пункт C4 ниже. `clip()` сам по себе — вторая половина дефекта:
  он превратил −0.2381 в зелёный 0.0, то есть **выдумал значение**.
  Это прямое нарушение правила «мера либо честна, либо отказывает».
- **Q3 (metrics/doctor/census/tui).** Принято как находка — пункт C3,
  список уже назван твоей таблицей.
- **Q4 (BACKLOG против P6).** Моя ошибка в ТЗ-57: `agent/BACKLOG.md`
  остаётся **координаторским** файлом, разрешения на него я больше не
  выдаю. Твой способ — назвать в отчёте строкой «B<N> закрыта
  коммитом <sha>», правку вношу я. Правка из ТЗ-57, уехавшая с
  эстафетой, засчитана.
- **Q5 (живая проба PDF-цепочки AU).** Да, **разрешаю**: до 15 живых
  запросов через `RequestGate` — пункт C5.
- **Q6 (`CvmProvider.resolve`).** Чинить — пункт C6. Заодно правило:
  адаптер в тесте, подменяющий метод провайдера, **обязан** падать,
  если у настоящего провайдера этого метода нет.
- **Q7 (три мигающих теста).** Засчитано как объяснённое, а не
  недоделанное: картина моего красного прогона (пункт 3 зелёный,
  пункт 11 красный, ровно эти три теста) — это и есть причина A5.1.
  Но подтверждается она прогоном, а не рассуждением: пункт C7.
- **Q8 (отчёт о пропущенном пункте BACKLOG).** Причина названа
  правильно, вопросов нет.

## C4. Знак 3.08 и clip(), который выдумывает ноль

**Done when:**
- `cvm-dfp.v2`: строка 3.08 приводится к знаку, принятому словарём мер;
  в провенансе видно, что знак нормализован, и чем — не догадкой;
- `effective_tax` AMBEV даёт настоящую ставку (≈0.238), закреплено
  золотым тестом, старое золото ТЗ-57 обновлено одним коммитом с
  объяснением в сообщении;
- `clip(...,0,0.5)` **убран**: значение вне полосы — это не 0.0 и не
  0.5, а отказ с причиной из `rusterm/reasons.py` (заведи токен, если
  подходящего нет). Assert на случай вне полосы;
- прогоном показано, что ни одна другая мера от снятия clip не
  позеленела и не покраснела молча: назови число изменившихся строк
  переписи по всем рынкам.

## C1. KR через DART

`agent/CONTEXT.md` §4 M8 всё ещё пишет «KR needs its key».
`RUSTERM_DART_KEY` в окружении есть с 13.09.2026 (§5) — строка
устарела либо канал не написан. Выясни, что из двух, и закрой.

**Done when:**
- `rusterm ingest` собирает одного эмитента KR: провайдер → сырой
  объект с провенансом → факты → снапшот;
- путь воспроизводим **офлайн** на записанном ответе в
  `tests/data/`, тест гоняет сбор без сети;
- `rusterm markets` показывает KR с честным статусом канала;
- перепись мер по образцу A2 из ТЗ-57: таблица «мера → значение либо
  причина» золотым тестом, причины — только токены `rusterm/reasons.py`;
- израсходованные запросы названы числом из `rusterm budget`;
- строка M8 в `agent/CONTEXT.md` приведена к факту.

## C2. B36: живая модель под гвардом цитат

ТЗ-53 W3 дала живой прогон. B36 просит большего: реальная модель
отвечает **через четыре read-only инструмента** и ни одно её число не
проходит мимо гварда цитат.

**Done when:**
- прогон помечен маркером `live` и по умолчанию не гоняется —
  дефолтный набор остаётся зелёным без сети и без ключа;
- не менее пяти вопросов: два с полными данными, два с серой мерой,
  один заведомо без данных. Для каждого зафиксировано, что вернулось:
  ответ с цитатой, отказ с причиной из словаря, или отказ гварда;
- ни одного числа в ответе без источника — assert на это, не на глаз;
- счётчики вызовов и стоимости после прогона названы числами из
  `rusterm status`;
- в отчёте строка «B36 закрыта коммитом <sha>» — правку BACKLOG вношу я (Q4).

## C3. Расхождения из таблицы A3

Строки «команда → как открывает», которые ТЗ-57 A3 выложила в отчёт и
не чинила. Почини их той же механикой, что `markets`.

**Done when:** каждая названная в REPORT-57 команда только-чтения
оставляет `git status --porcelain` пустым, по assert'у на команду;
команды, которые пишут, поведения не меняют — assert и на это.


## C5. AU: живая проба цепочки документов

Бюджет — до 15 запросов через `RequestGate`, числом из `rusterm budget`.

**Done when:** цепочка «объявление → тело документа» либо пройдена и
закреплена офлайн-тестом на записанном ответе, либо назван **точный**
отказ (код, URL-форма, что именно закрыто) и он оформлен причиной из
словаря. Платный тариф — сразу стоп по ADR-0018, с именем тарифа.

## C6. CvmProvider.resolve

**Done when:** `rusterm add --ticker <BR> --market BR` проходит без
AttributeError на записанном ответе; адаптер `_BR` из тестов ТЗ-56
убран или сведён к настоящему провайдеру; есть тест, который краснеет,
если у провайдера рынка нет `resolve`/`ticker_venues` —
для **всех** зарегистрированных рынков, а не только BR.

## C7. Подтвердить причину A5.1 прогоном, а не рассуждением

**Done when:**
- воспроизведён мой красный прогон: застейдженный остывший
  `STATE.json` + вложенный selfcheck без `I5_NESTED` -> пункт 3
  зелёный, пункт 11 красный, ровно `test_export_chat_writes_transcript_json`,
  `test_tui_has_no_sql_no_http`,
  `test_g2_g5_venue_from_exchange_file_and_404_is_an_answer`;
- тем же прогоном после ffeb518 — зелено;
- обе команды с выводом в отчёт;
- если картина **не** сходится — значит причина другая, и это пишется
  прямо: тогда пункт уходит в Disputed с тем, что измерено.

## Если всё закрыто

`agent/BACKLOG.md`, пункт S или M, названный в отчёте.

РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md
