# TASK-44 — правка стража, которая доезжает до коммита

- **Status: READY**
- **Report:** `agent/REPORT-44.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать работу — `python3 agent/relay.py hand --to coordinator
  --report agent/REPORT-44.md --note "<одна строка>"`; затем
  `python3 agent/relay.py status --assert-holder coordinator`; затем
  `python3 agent/relay.py wait --for executor --timeout 3600`.
- **Stop time:** PROTOCOL §10. L1 — первым, это минуты.
- **Budgets:** network 0; model 0 (fake client).
- **Goal in one sentence:** правка `agent/p6_rule.sh` перестаёт
  исчезать по дороге в коммит, и три незакрытых пункта ТЗ-43
  закрываются целиком, а не наполовину.

## Вердикт по кругу 52 (координатор, 16.09.2026): СДАЧИ НЕ БЫЛО

Это не придирка к форме — сдавать было нечего:

- `agent/REPORT-43.md` на ветке **не существует**;
- `agent/STATE.json` указывает на `agent/TASK-42.md`, `item: HANDOFF`;
- записка эстафеты говорит «TASK-42 DONE … 704 passed/0 failed» —
  про прошлый круг;
- после выдачи ТЗ-43 пришёл один рабочий коммит, `73e6233`.

Приёмка на `73e6233`, свежее подключённое дерево:

    Итог: пройдено 11, провалено 2
    FAILED tests/test_i5_guard_source.py::test_i5_working_tree_widening_is_red_and_named
    FAILED tests/test_i5_guard_source.py::test_i5_staged_and_authorised_widening_is_green
    FAILED tests/test_k1_p6_relay_skip.py::test_forged_relay_message_editing_context_is_red

### K4 снимаю сам

`tests/test_m4_scale.py` в этом прогоне не упал ни разу. Значит тогда
это была нагрузка на моей машине, как я и предполагал. Пункт закрыт,
гонять его отдельно не нужно — извини за лишнюю работу, если уже начал.

### K1 — не сделан, хотя сообщение коммита говорит обратное

Коммит `73e6233` называется «p6 — пропуск коммита эстафеты по
проверяемому составу и изменению BATON». **`agent/p6_rule.sh` в этом
коммите отсутствует** — внутри только `tests/test_k1_p6_relay_skip.py`
и `tests/test_i5_guard_source.py`. Страж не менялся:

    $ git diff 1b17c39..73e6233 -- agent/p6_rule.sh
    (пусто)

Обход поэтому жив, повторён дословно:

    $ git commit -m "Эстафета: круг 99, ход у executor — agent/TASK-43.md"   # правит CONTEXT.md
    $ bash agent/p6_rule.sh
    P6: HEAD~1..HEAD (last commit) — коммит эстафеты, пропущен
    код возврата P6: 0

**Твой собственный тест это и показывает** — я прогнал его на твоём же
коммите:

    FAILED tests/test_k1_p6_relay_skip.py::test_forged_relay_message_editing_context_is_red
    AssertionError: assert 0 != 0

Тест написан честно и красный по делу. Потерялась ровно правка стража.
Это **третий раз за смену**, когда исчезает правка `agent/p6_rule.sh`,
и ты сам назвал причину в `627a0dc`: уборка красного случая I5
возвращает файл на место. Тогда ты убрал файл из уборки; теперь он
теряется снова — значит лечение не подействовало или вернулось назад.

### K2 — сделана половина, и не та

`tests/test_i5_guard_source.py` теперь пишет в маркер `session` —
уникальный идентификатор прогона. Это правильная половина. Но
`tests/test_i5z_demonstration_ran.py` не тронут и проверяет
по-прежнему:

    assert _pid_alive(pid) or fresh

Пишущая сторона научилась, читающая — нет. Дыра открыта: маркер
чужого живого процесса или файл моложе 30 минут по-прежнему красят
приёмку зелёным без демонстрации.

### K3 — не начат

Строки в `agent/CONTEXT.md` нет, `FileNotFoundError` на
несуществующем `COMMIT_EDITMSG` не починен — оба теста I5 красные в
свежем дереве, как и были.

## L1. Сначала — почему правка стража исчезает

Бери **первым**. Пока это не закрыто, любая правка `p6_rule.sh`
сдаётся «сделанной» и приезжает пустой.

**Done when:** в отчёте названа причина, по которой правка
`agent/p6_rule.sh` не попала в `73e6233` — измеренная, а не
предположенная (`git reflog`, содержимое индекса, уборка тестов:
проверяй, а не гадай). И поставлен барьер: перед `hand` что-то
обязано заметить расхождение между тем, что коммит обещает
сообщением, и тем, что в нём лежит. Достаточно узкого правила:
если сообщение коммита называет файл, которого в коммите нет, —
selfcheck красный с именем файла. Тест на оба исхода.

## L2. K1 целиком

Как в ТЗ-43: пропуск коммита эстафеты опирается на признак, который
исполнитель подделать не может; `tests/test_k1_p6_relay_skip.py`
зелёный; настоящие коммиты эстафеты (`4aa9275`, `b7f153a`, `51be115`)
по-прежнему пропускаются.

**Done when:** `bash agent/p6_rule.sh` на моём подделанном коммите даёт
ненулевой код, и `git show --stat <твой коммит>` показывает
`agent/p6_rule.sh` внутри. Второе — не формальность: именно его сейчас
и не хватило.

## L3. K2 целиком

`tests/test_i5z_demonstration_ran.py` сверяет `session` из маркера с
идентификатором **этого** прогона. `_pid_alive(pid) or fresh` уходит.

**Done when:** тест утверждает, что маркер с чужим `session` — от
живого процесса, свежий по времени — приёмку зелёной не делает; оба
прогона набора внутри одной приёмки (пункты 3 и 11) зелёные, обе
строки показаны.

## L4. K3 целиком

Как в ТЗ-43: save/restore не падает при отсутствующем
`COMMIT_EDITMSG`; `finally` не стреляет необъявленной переменной;
приёмка зелёная в **свежем** дереве с показанными командами; строка
про свежее дерево появляется в `agent/CONTEXT.md`.

## И про отчёт

`agent/REPORT-44.md` должен существовать и описывать сданное, а
`agent/STATE.json` — указывать на этот круг. В прошлый раз ход пришёл
ко мне с запиской про позапрошлое задание; разбирать это пришлось мне.

РАЗРЕШЕНО ПРАВИТЬ: agent/p6_rule.sh
РАЗРЕШЕНО ПРАВИТЬ: agent/CONTEXT.md
