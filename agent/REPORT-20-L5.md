# REPORT-20-L5 — Files become pages, deterministically

## Done

- rusterm/manual/extract.py implements the F8 ① contract; the
  interface is untouched. Format by CONTENT: %PDF- -> pdf; PK ->
  zip container (docx by word/document.xml, xlsx by xl/workbook.xml,
  else format_unsupported:unknown_zip_container); else text family
  (json / csv / tsv / html / md / txt by shape).
- Optional libraries degrade to VALUES naming the package:
  format_unsupported:pypdf | python-docx | openpyxl - tested with the
  importer monkeypatched to None (absent) and for real (present).
- Untrusted-input guards, every one a VALUE not an exception:
  - MAX_INPUT_BYTES 256 MB; MAX_PAGE_CHARS 2 MB;
  - zip: <=4096 members, <=512 MB declared uncompressed - checked from
    the central directory BEFORE any decompression (zip bomb refused
    cheaply); test uses a 5000-member bomb (writestr cannot forge
    declared sizes - noted in the test);
  - archive members are NEVER written to disk by archive paths -
    parsing is in memory; the escape-member test proves nothing
    appears outside the data root;
  - XML via defusedxml when present (parser choice recorded in the
    module docstring); XXE document refused by value;
  - DEADLINE_SECONDS 60 per document (extract_timeout).
- PDF: pypdf layout mode so table rows read ACROSS (the rusconv
  lesson) - test builds a hand-written two-column PDF and asserts
  "fleet_size ... 42" on one line; a blank/scan page yields empty text
  and an all-empty document is no_text_layer; corrupt pdf is a value.
- Determinism: same file extracted twice - byte-identical pages, same
  sha256 (test).
- 17 tests, all passing; fixtures are generated by the tests
  (synthetic, N6); budget 0 requests - none used.

## Process slip, honest

- The L5 commit (e4f55f1) was made BEFORE the selfcheck passed - my
  chain staged the new test file after the check ran (P3 fired on the
  unstaged file, acceptance never ran in that chain). Selfcheck was
  run immediately after on the committed head: exit 0, 13/13, and it
  is that verified state the READY TO MERGE line below refers to.
  Third variant of the same chaining mistake this week; the discipline
  fix (selfcheck always inside &&, after staging) is applied from
  here on.

## What not to trust

- The hand-written two-column PDF is synthetic and simple; real-world
  PDFs with rotated text or CID fonts may need more than layout mode.
- html stripping is a stdlib HTMLParser pass - no JS rendering; a
  JS-only page yields little text by design (that is ②'s problem, not
  ①'s).

## Disputed

- (empty)

## HANDOFF

Lane:            L5
Branch:          agent/n3-L5
Status:          DONE
Items done:      all lane items incl. bomb/escape/XXE values tests,
                 across-reading, determinism
Items not done:  -
Zone respected:  yes (rusterm/manual/extract.py,
                 tests/test_manual_extract.py; no test data needed)
selfcheck:       exit 0 on the head, "Итог: пройдено 13, провалено 0"
Tests:           17 passed, 0 skipped (test_manual_extract.py)
Payload:         no tests/data/manual needed - fixtures are generated
Network:         0 of 0
Model calls:     0
Secrets:         nothing to leak; no key involved

READY TO MERGE: agent/n3-L5  e4f55f1  selfcheck exit 0  tests 17 passed
