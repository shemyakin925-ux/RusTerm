# TASK-78 — граница круга у L3 и акции VZ, которые payload всё-таки доказал

- **Status: READY**
- **Report:** `agent/REPORT-78.md`
- **Protocol:** `agent/PROTOCOL.md` (§12). Состояние: `agent/CONTEXT.md`.
- **Relay:** сдать — `python3 agent/relay.py --branch agent/night-11
  hand --to coordinator --report agent/REPORT-78.md --note "<строка>"`;
  **затем сразу** `python3 agent/relay.py --branch agent/night-11 wait
  --for executor --timeout 3600` — и не завершай сессию: команда
  блокирует до следующего ТЗ. Коды: 0 — твой ход, 2 — повтори wait,
  3 — пауза (стоп), 4 — цикл закрыт (стоп).
- **Stop:** дневной круг — по списку, не по часам.
- **Budgets:** network 0 (payload VZ уже в репозитории фикстурой).
- **Как работать:** вопросов не задавать, спорное — в Disputed и
  дальше; один пункт — один коммит с именем пункта в заголовке.
- **РАЗРЕШЕНО ПРАВИТЬ:** `tests/test_report_sections.py`,
  `rusterm/normalize/concepts.py`, `agent/CONTEXT.md`

## Y0. Вердикт по ТЗ-76: ПРИНЯТО целиком, спор принят против координатора

Приёмка на голове ветки в отдельном дереве: **«Итог: пройдено 13,
провалено 0», код 0**. Удалённых `assert` — 0; `acceptance.sh`,
`selfcheck.sh`, `githooks/`, `docs/` не тронуты.

**Зубы проверены не цитатой, а мутацией — координатором:**

| Что вернул координатор | Что покраснело |
|---|---|
| деление лога по пустой строке (прежний парсер) | `test_l3_finds_the_item_by_subject_with_files_attached`, плюс сам L3 |
| `setdefault` в `_sections` | три теста W2, включая `KeyError: 'HANDOFF #2'` |

Зубы настоящие. Отдельно засчитано, что `FAKE_LOG` — **литерал**, а не
живой прогон: страж перестал зависеть от истории ветки.

**W5 проверен по данным.** `rusterm/normalize/` не тронут — карту ты
действительно не расширял; в фикстуре (2.7 КБ) тега
`CommonStockSharesOutstanding` нет. Отказ честный.

### Спор принят — и он против координатора

Ты прав: **L3 не ограничен кругом.** `_git_log_name_only()` смотрит всю
историю ветки, поэтому `\bW3\b` закрывается коммитом ТЗ-53
(`d549f2a`, `892d4c1`, `94ed9fa` — проверено, они существуют). Мой
вывод прошлого круга «страж теперь различает» был слишком широким: `Z9`
ловился лишь потому, что такого пункта не было в истории **никогда**.
Идентификаторы пунктов между ТЗ повторяются, так что дыра рабочая, а
не теоретическая. Чинится в Y1 — и это моя ошибка, не твоя.

## Y1. L3 видит только свой круг

**Done when:**
- выбор коммитов у L3 ограничен кругом так же, как у G4
  (`_commit_for_items(ids, round)`): границей служит коммит
  «Эстафета: круг N», номер круга — из `agent/BATON.json`;
- зуб на саму границу: литеральный лог, где пункт закрыт коммитом
  **чужого, более раннего** круга, а в текущем круге его нет —
  страж обязан назвать пункт недостающим. Тест краснеет на
  нынешнем безграничном выборе — покажи красноту цитатой до правки;
- на настоящей истории ветки: пункт `W3` **не** должен считаться
  закрытым коммитами ТЗ-53; докажи прогоном, назвав, что видит
  страж до и после;
- `Z9`-зуб сохраняется, но перестаёт быть главным доводом: в отчёте
  одной строкой скажи, почему он сам по себе слаб.

## Y2. Акции Verizon: тег есть, просто другой

Payload доказал не отсутствие данных, а **другой маршрут**. В фикстуре
лежат: `dei:EntityCommonStockSharesOutstanding` (обложка отчёта),
`us-gaap:CommonStockSharesIssued` и `us-gaap:TreasuryStockCommonShares`.
Правило 9 соблюдено: payload в репозитории, тег доказан данными — так
что расширять карту теперь **законно**.

**Done when:**
- выбран и обоснован маршрут: либо `dei` с обложки, либо
  `issued − treasury`; **приоритет назови правилом**, а не вкусом, и
  запиши в докстроку — что берётся, когда доступны оба;
- если считаешь `issued − treasury` — это формула, значит ей место
  по правилам формул, с происхождением обоих слагаемых; расхождение
  двух маршрутов на одних данных измерь и назови числом;
- тест на фикстуре VZ: `shares_outstanding` получает значение, а не
  отказ; старый страж W5 переписывается **под новое поведение**, а не
  удаляется — покажи, что он теперь утверждает;
- меры, которые от этого оживают (`market_cap`, `pe`, `pb` и прочие
  на акцию), названы числом: было → стало по VZ на фикстуре;
- **если маршрут окажется негодным** (единицы, дата, дубль класса
  акций) — это результат: Blocked с измерением, карту не трогать.

## Y3. Что не делать

- Не трогать `agent/acceptance.sh`, `selfcheck.sh`, `p1_rule.sh`,
  `p6_rule.sh`, `githooks/`.
- Не править `PROTOCOL.md`, `BACKLOG.md`, `LAUNCH.md`, `TASK-*.md`.
- Тег в карту — только тот, что лежит в payload репозитория. Похожий
  по имени тег из головы по-прежнему запрещён (правило 9).

## Если всё закрыто

Дальше по очереди: `agent/TASK-77.md` (воспроизводимая база пяти
бумаг), затем `agent/TASK-73.md`, затем `agent/TASK-74.md`.
