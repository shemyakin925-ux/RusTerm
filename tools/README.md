# tools/ — инструменты разработки

Здесь живут только dev-инструменты вне приложения (обрезка записанных
ответов EDGAR и подобные); ничего из этого каталога не импортируется
кодом `rusterm/` и не проверяется приёмкой как пакет.
